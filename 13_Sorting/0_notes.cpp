/*
    sort(v.begin(),v.end()) :- sort array or vector in assecending order.
    swap(a,b) :- a or b ko swap kar dega
        ! ya dono in-built function jo <algorithm> header file ma aata ha.
        
    Q. How much maximum swap are needed to sort array of length 6?
    Ans:- total no. of swap = total no. of operation
                            = n(n-1)/2
*/